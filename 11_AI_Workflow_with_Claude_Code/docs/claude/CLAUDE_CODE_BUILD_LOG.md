# Claude Code build log

Cumulative summary of **harness** changes: **`CLAUDE.md`**, **`.claude/`**, and related root files. **Per-video detail** lives under **[`build-notes/`](build-notes/README.md)**.

---

<!-- Each `VIDEO_*` prompt file embeds a `log-claude-build` step before that module’s push/PR (position varies—e.g. VIDEO_3 runs it before the first MCP push). New modules add or replace their `## VIDEO_*` section per the skill. -->
